<h1>Sign In</h1>

<div class="field">
    <form action="/auth/signin" method="POST">

        <div class="field">
            <label for="email">Email</label>
            <input class="input" type="email" id="email" name="email" placeholder="Enter your email">
            </p>
        </div>

        <div class="field">
            <label for="password">Password</label>
            <input class="input" type="password" id="password" name="password" placeholder="Enter your password ">
        </div>

        <div class="field">
            <div class="control">
                <button type="submit" class="button is-success">Sign In</button>
                {{!-- <a class="button google is-primary" href="/login/federated/google">Sign in with Google</a> --}}
            </div>

            
    </form>
</div>